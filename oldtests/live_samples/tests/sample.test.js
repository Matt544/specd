test("print prints", () => {
  // Spec: `print` prints its argument to stderr [sample.spec.md]
});

test("print newline", () => {
  // Spec: `print` output ends with \\n [sample.spec.md]
});

test("print record separator", () => {
  // Spec: `print` separates records with \\n\\n [sample.spec.md]
});
